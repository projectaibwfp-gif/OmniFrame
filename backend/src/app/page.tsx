export default function HomePage() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: '3rem' }}>
      <h1>Project AI API</h1>
      <p>REST API działa. Sprawdź <code>/api/health</code> lub <code>/api/products</code>.</p>
    </main>
  );
}

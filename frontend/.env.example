# API base URL. Optional:
# - dev (vite): defaults to /api (Vite proxy -> backend)
# - production build: defaults to https://apiomniframe.vercel.app/api
# Override only if your backend runs elsewhere.
# VITE_API_BASE_URL=/api
VITE_GOOGLE_CLIENT_ID=************************************************************************
